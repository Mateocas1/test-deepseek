import { query as q, mutation as m, action as a } from "convex/server";

export const query = q;
export const mutation = m;
export const action = a;
export const internalQuery = q;
export const internalMutation = m;
export const internalAction = a;
